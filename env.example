# ============================================
# AlgoShield - Environment Variables Example
# ============================================
# Copy this file to .env and update with your actual values
# For environment-specific configs, see:
# - .env.development (local development)
# - .env.production (production deployment)
# - .env.test (testing and CI/CD)
#
# IMPORTANT: Never commit actual secrets to version control
# Use environment-specific .env files or a secrets management system

# ============================================
# General Configuration
# ============================================
# Options: development, production, test
ENVIRONMENT=development
# Options: debug, info, warn, error
LOG_LEVEL=info

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=algoshield
# REQUIRED: Must be at least 16 characters
# In production: must contain at least 3 of: uppercase, lowercase, digits, special chars
POSTGRES_PASSWORD=your_secure_password_here_min_16_chars
POSTGRES_DB=algoshield

# ============================================
# Redis Configuration
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379

# ============================================
# API Configuration
# ============================================
API_HOST=0.0.0.0
API_PORT=8080

# ============================================
# TLS/HTTPS Configuration
# ============================================
# REQUIRED in production: Set to true to enable TLS/HTTPS
# In production, TLS is mandatory for security (prevents man-in-the-middle attacks)
TLS_ENABLE=false
# Path to TLS certificate file (PEM format)
# Example: /etc/ssl/certs/api.crt
TLS_CERT_PATH=
# Path to TLS private key file (PEM format)
# Example: /etc/ssl/private/api.key
TLS_KEY_PATH=

# ============================================
# Worker Configuration
# ============================================
# Number of concurrent worker goroutines
WORKER_CONCURRENCY=10
# Number of transactions to process in a batch
WORKER_BATCH_SIZE=50

# ============================================
# Authentication Configuration
# ============================================
# REQUIRED: Must be at least 32 characters
# In production: must contain at least 3 of: uppercase, lowercase, digits, special chars
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_minimum_32_characters_long
# JWT token expiration time in hours
JWT_EXPIRATION_HOURS=24
