import { describe, it, expect } from 'vitest'

describe('vuetify plugin', () => {
  describe('updateVuetifyTheme function', () => {
    it('exists and can be tested for type', () => {
      expect(true).toBe(true)
    })
  })

  describe('vuetify instance', () => {
    it('exists and can be tested for type', () => {
      expect(true).toBe(true)
    })
  })
})
