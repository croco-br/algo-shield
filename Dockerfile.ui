# =============================================================================
# Multi-stage Dockerfile for AlgoShield UI (Next.js)
# =============================================================================
# Stage 1: Dependencies stage
# =============================================================================
FROM node:20-alpine AS deps

# Install build dependencies
RUN apk add --no-cache libc6-compat

# Set working directory
WORKDIR /app

# Copy package files
COPY src/ui/package.json src/ui/package-lock.json ./

# Install dependencies
# Use npm ci for faster, reliable, reproducible builds
RUN npm ci --only=production && \
    npm cache clean --force

# =============================================================================
# Stage 2: Builder stage
# =============================================================================
FROM node:20-alpine AS builder

WORKDIR /app

# Copy dependencies from deps stage
COPY --from=deps /app/node_modules ./node_modules

# Copy source code
COPY src/ui/ ./

# Set build-time environment variables
# Next.js requires these to be set at build time for static optimization
ARG NEXT_PUBLIC_API_URL
ARG API_URL
ENV NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
ENV API_URL=${API_URL}
ENV NODE_ENV=production
ENV NEXT_TELEMETRY_DISABLED=1

# Build Next.js application
RUN npm run build

# =============================================================================
# Stage 3: Runner stage
# =============================================================================
FROM node:20-alpine AS runner

WORKDIR /app

# Set production environment
ENV NODE_ENV=production
ENV NEXT_TELEMETRY_DISABLED=1

# Create non-root user
RUN addgroup -S appgroup && \
    adduser -S appuser -G appgroup

# Copy necessary files from builder
# Next.js standalone output structure:
# .next/standalone/ contains server.js and node_modules
# .next/static/ contains static assets
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
COPY --from=builder /app/public ./public

# Set proper ownership
RUN chown -R appuser:appgroup /app

# Switch to non-root user
USER appuser:appgroup

# Expose port
EXPOSE 3000

# Set environment variable for Next.js
ENV PORT=3000
ENV HOSTNAME="0.0.0.0"

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=20s --retries=3 \
    CMD wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1

# Start Next.js server
# Next.js standalone places server.js in .next/standalone/server.js
# But we copied .next/standalone to root, so server.js is at root
CMD ["node", "server.js"]
